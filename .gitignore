node_modules
.DS_Store
dist
build
.cache
.turbo
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
*.log
.env
.env.local
coverage
.vitest
*.tsbuildinfo

# TanStack Router writes this on dev/build but it's fine to commit it
# (faster cold-start, deterministic builds). Leaving uncommented note.
!routeTree.gen.ts
