{
  "name": "@data-agent/api-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@data-agent/db": "workspace:*",
    "@data-agent/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.86.0"
  }
}
